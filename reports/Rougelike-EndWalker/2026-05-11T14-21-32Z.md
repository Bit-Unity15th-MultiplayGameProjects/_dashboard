---
project: "Rougelike-EndWalker"
date: "2026-05-11T06:30:00+00:00"
commit_range: "ce8cc2f31cb6386c2948f8124b00a059fbdb6525..1113af0b628f1729f91a2f8a50346866ad200487"
commit_count: 1
risk_level: "high"
tags:
  - "멀티플레이"
  - "커스텀-네트워크"
  - "동기화"
  - "문서화"
  - "기술부채"
summary: "멀티 캐릭터 동기화는 전진했지만 런타임 안정성 위험이 높다."
progress_estimate: 48
doc_scores:
  design: 5
  technical: 5
  spec: 4
todos:
  - title: "2인 Game 씬 진입부터 S_ObjectSpawned 수신까지 재현 테스트 고정"
    priority: "high"
    files:
      - "Client/Assets/Scripts/App/BattleSceneController.cs"
      - "Server/Server/InGame/RoomService.cs"
      - "Client/Assets/Scripts/Network/InGame/NetworkObjectManager.cs"
    details: "C_EnterNode 기반 스폰이 로그 의존이라 happy path 실패 원인이 숨는다."
  - title: "RoomService 퇴장 처리에서 master 갱신과 NetworkObject despawn 완료"
    priority: "high"
    files:
      - "Server/Server/InGame/RoomService.cs"
    details: "_masterClientId와 스폰 상태가 남아 재접속·씬 재진입 때 ghost 상태가 생긴다."
  - title: "NetworkPlayerTransform 송신 주기와 보간·임계값 규칙 도입"
    priority: "high"
    files:
      - "Client/Assets/Scripts/Network/InGame/NetworkPlayerTransform.cs"
    details: "Vector3 변화마다 TCP 송신해 이동 중 매 프레임 패킷과 원격 순간이동이 발생한다."
  - title: "SendMessage 기반 네트워크 제어를 명시 인터페이스 호출로 교체"
    priority: "medium"
    files:
      - "Client/Assets/Scripts/Network/InGame/NetworkObjectManager.cs"
      - "Client/Assets/Scripts/Network/InGame/NetworkPlayerTransform.cs"
    details: "문자열 메서드 호출은 리팩터링에 취약하고 DontRequireReceiver로 실패가 묻힌다."
  - title: "패킷 프로토콜 버전·호환성 오류 응답을 PacketIO에 추가"
    priority: "medium"
    files:
      - "Client/Assets/Scripts/Network/Runtime/PacketIO.cs"
      - "Client/Assets/Scripts/Network/Runtime/PacketFactory.cs"
    details: "Unknown packet skip은 디버깅용으로는 좋지만 서버·클라 버전 불일치를 숨긴다."
  - title: "멀티플레이 PlayMode 검증 절차와 자동 테스트 최소 1개 추가"
    priority: "medium"
    files:
      - "Client/Assets"
      - "Visualizer Doc/6.networkerror fixed.md"
    details: "패키지만 추가되고 재현 절차가 없어 회귀가 사람 로그 확인에 묶여 있다."
backlogs:
  - title: "테스트 코드·플레이모드 검증 절차 부재"
    priority: "high"
    files:
      - "Client/Assets"
  - title: "패킷 프로토콜 스키마와 버전 호환성 규칙 부재"
    priority: "high"
    files:
      - "Client/Assets/Scripts/Network/Core/Packet.cs"
      - "Client/Assets/Scripts/Network/Core/PacketId.cs"
  - title: "문서 위치 이동 후 기존 참조 경로 검증 부재"
    priority: "medium"
    files:
      - "Doc"
      - "Visualizer Doc"
  - title: "확률 노드 가중치·BO 쿨다운 2 스테이지의 근거 수치 부재"
    priority: "medium"
    files:
      - "Client/Assets/Resources/Data/Stage/stage_config.json"
      - "Doc"
  - title: "사서·기록자·최종 보스 등 핵심 인물 특성·밸런싱 수치 미기재"
    priority: "medium"
    files:
      - "Doc"
  - title: "아트/UI 컨셉 테이블에 placeholder(`[예: Hi-Bit ...]`) 잔존"
    priority: "low"
    files:
      - "README.md"
  - title: "README 작성자 표기 `차영록`/`차용록` 혼용"
    priority: "low"
    files:
      - "README.md"
resolved_from_backlog: []
---

# Rougelike-EndWalker — 멀티플레이 동기화 스냅샷

## 1. 주요 변경사항
- 로비와 게임 씬에 `NetworkObjectManager`를 배치하고, Knight/Veteran 프리팹에 네트워크 동기화 컴포넌트를 추가했다.
- 캐릭터 선택, 테마 선택, 인원 표시가 서버 패킷과 연결되었다. 단순 UI 상태에서 실제 룸 상태 반영으로 한 단계 전진했다.
- `C_EnterNode`를 통해 Game 씬 진입 후 서버가 선택 캐릭터를 스폰하도록 흐름을 바꿨다.
- `PacketIO`에 최대 패킷 크기 상수를 두고, 알 수 없는 패킷을 스킵하도록 변경했다.
- 멀티플레이 디버깅 기록 문서를 추가했으나, 문서 내용 일부가 현재 코드와 어긋난다.

## 2. 코드 품질 리뷰
이번 변경의 방향은 좋다. 로비 선택값을 서버에 저장하고 Game 씬에서 다시 스폰하는 구조는, 선택 UI와 실제 전투 오브젝트를 분리하려는 의도가 보인다. 그러나 구현은 아직 디버그 로그와 런타임 메시지 호출에 강하게 기대고 있다. `NetworkObjectManager`의 `BroadcastMessage("StopRotating")`, `NetworkPlayerTransform`의 `SendMessage("Enable", 5f)`는 컴파일 타임 검증이 되지 않는다. 왜 문제냐면 메서드명 변경, 컴포넌트 제거, 프리팹 구조 변경이 런타임 침묵 실패로 이어진다. 개선은 `INetworkSpawnView`, `IPlayerControlGate` 같은 작은 인터페이스나 명시 컴포넌트 참조로 바꾸는 쪽이 맞다.

서버 쪽은 더 위험하다. `RoomService.RemoveSession`은 새 마스터 후보를 계산하지만 `_masterClientId`를 갱신하지 않는다. 또한 `_characterSelections`, `_lobbySpawnedObjects`, `_enteredClients`, 실제 NetworkObject despawn 정리가 함께 보이지 않는다. 왜 문제냐면 2인 happy path는 통과해도 재접속, 퇴장, 씬 재진입 순간에 ghost object와 권한 불일치가 발생한다. 이 부분은 기능 추가보다 먼저 정리해야 한다.

자료구조/복잡도 관점에서는 `NetworkPlayerTransform.Update()`가 핵심 지적이다. 현재는 owner 위치가 `Vector3` 단위로 조금이라도 바뀌면 매 프레임 `SetAndSend`가 호출된다. 비용은 이동 중 매 프레임 TCP 패킷 생성·직렬화·브로드캐스트이며, 원격 보간도 없어 화면 흔들림으로 이어진다. 추천 흐름은 송신 tick, 위치 변화 임계값, 마지막 전송값 캐시, 원격 보간 버퍼를 두는 것이다. 커스텀 네트워크 프로젝트에서는 이 비용이 곧 시연 안정성이다.

## 3. 진행도 평가
이 프로젝트는 `custom-network` 경로다. Unity NGO 패키지가 존재하지만 실제 핵심은 자체 서버, 패킷 ID, `NetworkStream`, 룸 서비스, 직접 직렬화로 구성되어 있다. 따라서 네트워크 기반 공사는 진척으로 인정한다. 캐릭터 선택부터 transform sync까지 잇는 시도는 낮은 수준의 수직 슬라이스에 가까워졌다.

다만 제출 가능한 반복 시연 단계로 보기는 어렵다. 현재 스폰 성공 여부가 로그 확인에 의존하고, 퇴장·재진입·버전 불일치·패킷 호환성 대응이 약하다. 진행도는 48로 본다. 어려운 경로를 감안해 컨텐츠 부족은 일부 보정하지만, 네트워크 불안정성 때문에 `risk_level`은 high가 타당하다.

## 4. 다음 권장사항
- 2인 Game 씬 진입부터 `S_ObjectSpawned` 수신, owner 이동, remote 반영까지 한 번에 검증하는 절차를 고정해야 한다.
- `RoomService`에서 퇴장 시 master 갱신, 선택 상태 제거, 로비/게임 스폰 오브젝트 despawn을 한 트랜잭션처럼 처리해야 한다.
- `NetworkPlayerTransform`은 매 프레임 송신을 멈추고 tick rate, 임계값, 원격 보간을 명시해야 한다.
- `SendMessage`와 `BroadcastMessage`는 명시 인터페이스 호출로 바꿔 런타임 침묵 실패를 제거해야 한다.
- `PacketIO`의 unknown packet skip은 버전 불일치 응답과 연결해 프로토콜 계약으로 승격해야 한다.

## 5. 문서화 상태
design 점수는 5다. 세계관과 캐릭터, 테마 방향은 일부 존재하는 것으로 보이나, 이전 backlog의 핵심 인물 특성·밸런싱 수치 공백이 유지된다. 선택의 이유와 플레이 경험 목표가 아직 사양 수준으로 닫히지 않았다.

technical 점수는 5다. `Visualizer Doc/6.networkerror fixed.md`는 버그 흐름과 동기화 절차를 남긴 점이 좋다. 그러나 문서에는 `HandleSelectCharacter`에서 spawn 제거라고 적혀 있는데 실제 diff는 로비 spawn을 수행한다. 이런 불일치는 신규 기여자를 더 헷갈리게 만든다.

spec 점수는 4다. 패킷 스키마, 버전 호환성, PlayMode 검증, 밸런싱 근거 수치가 여전히 부족하다. 테스트 체크리스트로 바로 쓰기에는 필드, 상태, 기대 결과가 모자라다.

## 6. Backlog
- 테스트 코드·플레이모드 검증 절차 부재: Multiplayer PlayMode 패키지는 들어왔지만 실제 검증 절차와 테스트 코드는 아직 없다.
- 패킷 프로토콜 스키마와 버전 호환성 규칙 부재: unknown packet 처리만으로는 계약이 정의되지 않는다.
- 문서 위치 이동 후 기존 참조 경로 검증 부재: `Doc`, `Visualizer Doc` 간 참조 무결성 확인 근거가 없다.
- 확률 노드 가중치·BO 쿨다운 2 스테이지의 근거 수치 부재: 밸런싱 판단을 재현할 수 없다.
- 사서·기록자·최종 보스 등 핵심 인물 특성·밸런싱 수치 미기재: 콘텐츠 기준선이 불명확하다.
- README 아트/UI 컨셉 placeholder 잔존: 외부 평가자가 완성도를 낮게 본다.
- README 작성자 표기 혼용: 작은 문제지만 제출 문서 신뢰도를 깎는다.