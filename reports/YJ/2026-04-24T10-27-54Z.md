---
project: "YJ"
date: "2026-04-24T00:00:00+00:00"
commit_range: "c9cb3dc80884b0871d2821cc71cc94083add816e..e94bf222b60043a465a57d2e15a025768c08cc8b"
commit_count: 3
risk_level: "high"
tags:
  - "핵심로직"
  - "보드게임"
  - "입력처리"
  - "기술부채"
  - "문서부족"
summary: "핵심 플레이 로직은 생겼으나 구조 부채와 룰 검증 공백이 크다."
progress_estimate: 28
doc_scores:
  design: 2
  technical: 1
  spec: 2
todos:
  - title: "PlayerController 의 선택·이동·포획·승리 판정을 역할별 클래스로 분리"
    priority: "high"
    files:
      - "Assets/Scripts/Controller/PlayerController.cs"
    details: "445줄 단일 컨트롤러가 게임 규칙과 입력을 함께 가져 변경 비용이 급증한다."
  - title: "보드 규칙을 순수 C# 서비스로 분리하고 EditMode 테스트 추가"
    priority: "high"
    files:
      - "Assets/Scripts/BoardManager.cs"
      - "Assets/Scripts/Piece.cs"
      - "Assets/Scripts/Controller/PlayerController.cs"
  - title: "포로 재배치·승격·입궁 승리 규칙을 문서화하고 코드와 대조"
    priority: "high"
    files:
      - "Assets/Scripts/Controller/PlayerController.cs"
  - title: "Input System 사용 여부를 결정하고 구 Input API 사용을 정리"
    priority: "medium"
    files:
      - "Assets/Scripts/Controller/PlayerController.cs"
      - "Assets/Scenes/Game.unity"
  - title: "깨진 한글 주석 인코딩을 복구하고 의미 있는 주석만 남기기"
    priority: "medium"
    files:
      - "Assets/Scripts/Controller/PlayerController.cs"
  - title: "결과 UI 참조와 승패 표시 흐름을 하나로 정리"
    priority: "medium"
    files:
      - "Assets/Scripts/Controller/PlayerController.cs"
      - "Assets/Scenes/Game.unity"
backlogs:
  - title: "GDD/TDD 본문 다수가 '[입력하세요]' placeholder 로 비어있음"
    priority: "high"
  - title: "네트워킹 스택(Mirror/NGO/Photon) 미선정 — 멀티플레이 게임 핵심 의사결정 보류"
    priority: "high"
  - title: "씬은 SampleScene 하나뿐, 매칭/대기실/인게임 씬 분리 설계 없음"
    priority: "medium"
    files:
      - "Assets/Scenes/Game.unity"
  - title: "포로 재배치 시 '상대 진영 첫 줄' 금지 규칙은 일본 쇼기와 다름 — 룰 출처 재확인 필요"
    priority: "medium"
    files:
      - "Assets/Scripts/Controller/PlayerController.cs"
  - title: "concept.md 의 도식 코드블록(```)이 닫히지 않은 채 '#### 핵심 테마'로 이어짐"
    priority: "low"
    files:
      - "concept.md"
resolved_from_backlog:
  - title: "Unity C# 코드 자체가 0줄 (TutorialInfo 샘플 외) — 핵심 로직 부재"
    files:
      - "Assets/Scripts/BoardManager.cs"
      - "Assets/Scripts/Controller/PlayerController.cs"
      - "Assets/Scripts/Piece.cs"
---

# YJ — 핵심 로직 1차 구현 스냅샷

## 1. 주요 변경사항
- `Game.unity` 씬이 추가되고 `SampleScene` 이 제거되었다. 보드, 말 프리팹, 결과 패널, 게임 매니저가 한 씬에 배치되었다.
- 말 이미지, 프리팹, `PieceDataSO` 에셋이 추가되어 최소한의 데이터 기반 말 구성이 시작되었다.
- `BoardManager`, `Piece`, `PlayerController` 가 추가되어 선택, 이동, 포획, 포로 재배치, 승격, 왕 포획 승리, 입궁 승리 흐름이 구현되었다.
- TextMesh Pro 패키지 리소스가 대량 추가되었다. 기능상 필요할 수 있으나 변경량 대부분을 차지해 실제 게임 로직 변경이 묻힌다.

## 2. 코드 품질 리뷰
가장 큰 문제는 `PlayerController` 가 입력 처리, 턴 관리, 보드 상태 변경, 포획 처리, 승격, 승리 판정, UI 출력까지 모두 가진다는 점이다. 지금은 445줄이지만, 룰 하나를 고칠 때마다 입력·UI·상태 변경 사이드이펙트를 같이 확인해야 한다. 보드게임류에서는 규칙 엔진을 순수 C# 클래스로 분리하고 MonoBehaviour 는 입력과 표시만 담당하게 만드는 것이 장기적으로 훨씬 안전하다.

```csharp
if (Input.GetMouseButtonDown(0)) HandleMouseDown();
if (Input.GetMouseButton(0)) HandleMouseDrag();
if (Input.GetMouseButtonUp(0)) HandleMouseUp();
```

씬에는 `InputSystemUIInputModule` 이 들어갔지만 코드에서는 구 `Input` API 를 직접 사용한다. 왜 문제냐면 UI 입력과 게임 입력의 기준이 갈라져 모바일·패드·멀티플레이 입력으로 확장할 때 같은 행동을 두 번 구현하게 된다. 개선하려면 이번 단계에서 Input System 기반 액션으로 통일하거나, 최소한 "현재는 마우스 전용 프로토타입"이라고 명시해야 한다.

또한 한글 주석이 인코딩 깨짐 상태로 커밋되어 있다. 주석은 유지보수자를 돕는 도구인데, 현재 상태는 오히려 코드 이해를 방해한다. 의미 없는 깨진 주석은 제거하고, 규칙상 애매한 부분만 정상 인코딩으로 짧게 남겨야 한다. 결과 UI도 `resultTitleText` 와 `resultDetailText` 가 같은 TMP 오브젝트를 참조하고 있어 상세 문구가 제목을 덮어쓸 수 있다. 이런 문제는 기능이 늘기 전에 바로 정리해야 한다.

## 3. 진행도 평가
이전 대비 가장 큰 전진은 실제 Unity C# 로직과 프리팹·데이터 에셋이 생겼다는 점이다. 이제 "프로젝트가 비어 있다" 단계는 벗어났다. 다만 구현은 아직 플레이 가능한 수직 슬라이스 초입이며, 룰 검증·테스트·문서화·네트워크 방향성은 거의 그대로 남아 있다.

진행도는 28%로 본다. 핵심 조작 루프 일부는 들어왔지만, 현재 구조로 계속 기능을 얹으면 디버깅 비용이 빠르게 커진다. 다음 스냅샷 전까지는 기능 추가보다 규칙 로직 분리와 테스트 기반 확보가 우선이다.

## 4. 다음 권장사항
- `PlayerController` 를 입력, 턴 진행, 룰 판정, 결과 UI 클래스로 나눈다.
- 보드 이동·포획·승격·포로 재배치를 순수 C# 규칙 서비스로 옮기고 EditMode 테스트를 붙인다.
- 포로 재배치, `자 -> 후` 승격, 입궁 승리 조건을 문서로 확정하고 코드와 맞춘다.
- Input System 사용 여부를 결정해 씬 설정과 코드 입력 방식을 일치시킨다.
- 깨진 한글 주석과 결과 UI 참조를 정리해 런타임 표시 오류를 줄인다.

## 5. 문서화 상태
design 점수는 2점이다. 이번 변경에서 게임의 의도, 코어 루프, 승리 조건의 설계 근거를 보강한 문서 변경이 확인되지 않는다. 코드상으로는 룰이 생겼지만 "왜 이 규칙인가"가 문서로 남아 있지 않다.

technical 점수는 1점이다. 아키텍처 개요, 클래스 책임, 데이터 흐름, 입력 처리 정책, 네트워크 권한 모델이 없다. 신규 개발자가 `PlayerController` 를 직접 읽어야만 전체 구조를 파악할 수 있다.

spec 점수는 2점이다. 말 데이터와 프리팹은 생겼지만 룰 사양서, 좌표계 정의, 이동 가능 조건, 포로 배치 제한, 승리 판정 체크리스트가 부족하다. 테스트 기준으로 쓰기에는 아직 미흡하다.

## 6. Backlog
- GDD/TDD placeholder 문서가 남아 있다. 구현이 시작된 만큼 문서 공백은 이제 단순 미작성보다 더 큰 리스크다.
- 네트워킹 스택은 아직 미정이다. 코드 주석에는 Photon 언급이 있으나 실제 권한 모델은 없다.
- 씬은 `Game.unity` 하나로 대체되었을 뿐, 매칭·대기실·인게임 분리 설계는 해결되지 않았다.
- 포로 재배치 제한과 입궁 승리 조건은 코드에 들어갔지만 룰 출처와 의도 검증이 없다.
- `concept.md` 코드블록 문제는 이번 변경에서 해결 근거가 확인되지 않는다.

## 7. 이전 Backlog 해결
- Unity C# 코드 자체가 0줄이던 문제는 해결되었다. `BoardManager`, `Piece`, `PlayerController` 가 추가되어 보드게임의 핵심 조작 루프와 승리 판정이 코드로 들어왔다.